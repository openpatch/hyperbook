---
"@hyperbook/types": minor
"@hyperbook/fs": minor
"hyperbook": minor
---

Add `title` as an alternative to `name` in page and section frontmatter for better compatibility with other documentation tools like MkDocs. When both are present, `name` takes precedence.
