---
"@hyperbook/markdown": patch
"hyperbook": patch
---

**pyide**, **p5**, **openscad**: The reset, copy and download buttons in the editor toolbar are now icons (`↺`, `⧉`, `⇩`) instead of written labels, which wrapped onto several lines in languages with long words. The wording moves to `title` and `aria-label`, so it still shows as a tooltip and is still announced by screen readers.
