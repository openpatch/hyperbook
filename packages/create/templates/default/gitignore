.hyperbook
out
public/archives/

# Holds the actual passwords used by :::protect and `protect:` frontmatter.
# Committing it would defeat the point on a public book. Supply the values in
# CI through HYPERBOOK_PASSWORDS or HYPERBOOK_PASSWORD_<KEY> instead.
passwords.json
