---
name: Einweg Vorlagen
lang: de
---

# Einweg Vorlagen

Einweg Vorlagen können für jede Seite verwendet werden. Wenn eine Seite
anstelle auf `.md` auf `.md.hbs` endet, dann wird die Seite vorher
interpretiert und alle Funktionen die für Vorlagen verfügbar sind, sind
auch hier verfügbar.

Ein Beispiel ist diese Dokumentationsseite.

```hbs
{{ uppercase kleingeschrieben }}
```

{{ uppercase kleingeschrieben }}
