---
"@hyperbook/markdown": minor
"hyperbook": minor
---

Add Kiri:Moto directive for embedding the browser-based 3D slicer from grid.space. Supports `mode`, `model`, `workspace`, `settings`, and `height` attributes. Global defaults for `height` and `settings` can be set in `hyperbook.json`.
