<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>{{group.name}} — Student Credentials</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; padding: 20px; }
    h1 { font-size: 1.2rem; margin-bottom: 16px; }
    .toolbar { margin-bottom: 16px; }
    .toolbar a, .toolbar button {
      padding: 8px 16px;
      cursor: pointer;
      border: 1px solid #ccc;
      border-radius: 4px;
      background: #f5f5f5;
      text-decoration: none;
      color: black;
      font-size: 0.9rem;
      margin-right: 8px;
    }
    .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
    .card { border: 2px solid #333; border-radius: 6px; padding: 12px 14px; break-inside: avoid; }
    .card .label { font-size: 0.7rem; text-transform: uppercase; color: #666; margin-bottom: 2px; }
    .card .value { font-size: 0.95rem; font-weight: 600; font-family: monospace; }
    .card .username { margin-bottom: 8px; }
    @media print {
      body { padding: 0; }
      h1 { font-size: 1rem; }
      .toolbar { display: none; }
      .grid { gap: 8px; }
      .card { border-width: 1.5px; padding: 10px 12px; }
    }
  </style>
</head>
<body>
  <h1>{{hyperbook.name}} — {{group.name}}</h1>

  <div class="toolbar">
    <button onclick="window.print()">🖨 Print</button>
    <a href="/hyperbook/{{hyperbook.id}}/group/{{group.id}}">← Back to Students</a>
  </div>

  {{#if students.length}}
  <div class="grid">
    {{#each students}}
    <div class="card">
      <div class="username">
        <div class="label">Username</div>
        <div class="value">{{this.username}}</div>
      </div>
      <div>
        <div class="label">Password</div>
        <div class="value">{{this.password}}</div>
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <p>No students in this group.</p>
  {{/if}}
</body>
</html>
